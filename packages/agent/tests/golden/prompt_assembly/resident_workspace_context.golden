COLLECTIONS: Engineering, Q3 Planning

MENTIONED ENTITIES (already resolved — use these ids directly, do not ask for one):
- "Northwind Trading" (company_sold_to) id=01J8ZQ3K7X2N4P6R8T0V2W4Y6A
(more matches not shown: at least 1 — this list is not exhaustive; look a name up before treating it as unique or absent)

EXISTING SCHEMAS (do not recreate these; do not copy their fields onto a new type):
- ticket "Ticket" -> title: text, required; status: enum {ready_for_dev, in_dev, ready_for_review, in_review, done}, required; assignee: text; sprint: text [title_template: {title}]
- adr "ADR" -> title: text, required; status: enum {proposed, accepted, superseded}, required; supersedes: text

RELATED (via relationship, not directly matched):
- release: Release

ACTIVE PLAYBOOKS:
- "Sprint Close-Out": When a ticket's status -> done, check whether its sprint is fully closed.
- "Weekly Triage"
